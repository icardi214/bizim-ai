from flask import Flask, render_template_string, request
import os

app = Flask(__name__)

html = """
<!DOCTYPE html>
<html>
<head>
    <title>Abdulkadir AI</title>
</head>
<body>
    <h1>🤖 Abdulkadir AI</h1>
    <form method="post">
        <input name="soru" placeholder="Bir şey yaz..." required>
        <button type="submit">Gönder</button>
    </form>
    <p>{{ cevap }}</p>
</body>
</html>
"""

@app.route("/", methods=["GET", "POST"])
def home():
    cevap = ""
    if request.method == "POST":
        soru = request.form["soru"]

        if "nasılsın" in soru.lower():
            cevap = "İyiyim 😎"
        elif any(op in soru for op in "+-*/"):
            try:
                cevap = str(eval(soru))
            except:
                cevap = "Matematik hatası 🤔"
        else:
            cevap = "Bunu henüz bilmiyorum."

    return render_template_string(html, cevap=cevap)

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=int(os.environ.get("PORT", 5000)))
